import 'package:flutter/material.dart';

class WalletFragment extends StatelessWidget {
  const WalletFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Wallet screen works!'),
    );
  }
}
